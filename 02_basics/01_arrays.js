//1) declare methods
const arr1 = ['a', 'b', 'c']; // shortcut for new Array(). called an array literal/array shorthand.
const arr2 = new Array('a', 'b', 'c');
// console.log('arr1 =>',arr1,' arr2 =>', arr2, '. They are fundamentally similar.');

const arr3 = new Array(3);
console.log('arr3 =>',arr3 , 'arr3[0] =>', arr3[0], 'arr3.length =>',arr3.length," new Array(3) declares 3 undefined(empty) elements." );

